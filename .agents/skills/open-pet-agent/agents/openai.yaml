interface:
  display_name: "Open Pet Agent"
  short_description: "Repo-specific patterns and workflows for open-pet-agent"
  default_prompt: "Use the open-pet-agent repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true